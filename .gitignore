# Claude Code / OpenSpec local tooling (regenerable via `openspec init --tools claude`)
.claude/

# Node / Electron
node_modules/
dist-electron/

# Local env & OS cruft — never commit secrets or macOS metadata
.env
.env.*
.DS_Store

# Docs site build (see .github/workflows/docs.yml). The intermediate MkDocs source
# tree and any local site output are regenerable; docs/ itself is published by CI.
.wiki-build/
site/
